Nginx + uWSGI + Flask on Dockerのテンプレート

# Usage

```
$ git clone git@github.com:c8112002/docker-nginx-uwsgi-flask-boilerplate.git
$ cd git@github.com:c8112002/docker-nginx-uwsgi-flask-boilerplate.git
$ docker-compose up -d
```
